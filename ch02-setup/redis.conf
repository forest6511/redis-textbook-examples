# 第2章 最小 redis.conf 設定例
# 公式のフル設定は redis.io / config-file を参照

# 待ち受けポート(既定 6379)
port 6379

# 待ち受けアドレス。開発はローカルのみに絞ると安全
bind 127.0.0.1 -::1

# パスワード認証。本番では必ず設定する(ここではコメントアウト)
# requirepass your-strong-password

# AOF 永続化を有効にする(再起動時にデータを復元するため)
appendonly yes
